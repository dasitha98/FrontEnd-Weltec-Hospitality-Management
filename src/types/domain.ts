export type ID = string;

export interface Student {
  id: ID;
  name: string;
  email: string;
}

export interface Class {
  id: ID;
  name: string;
}

export interface Food {
  id: ID;
  name: string;
}
